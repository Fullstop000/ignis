# Rust
/target/
**/*.rs.bk
# Cargo.lock is committed: this workspace ships a binary application.

# IDE / Editors
.idea/
.vscode/
*.swp
*.swo
*~
*.iml

# OS
.DS_Store
Thumbs.db
desktop.ini

# Environment
.env
.env.local
.env.*.local

# Backup / Merge
*.bak
*.orig

# Profiling / Coverage
*.profraw
grcov.profraw*
*.profdata

# Logs
*.log
raw_run.log

# Project specific
.context/

# Local Ignis config — may contain API keys. Use config.example.toml as a template.
/config.toml
/config.yaml

# Local Ignis runtime state (per machine, may reference local paths)
/state.json

# Claude Code — per-machine local state (shared skills/ stay tracked)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# MCP / tooling caches
.code-review-graph/
